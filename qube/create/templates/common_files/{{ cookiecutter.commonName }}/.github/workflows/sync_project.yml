name: Sync qube project

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Setup Python
          uses: actions/setup-python@v1
          with:
            python-version: 3.8

        - name: Install qube
          run: pip install qube

        - uses: actions/checkout@v2
          with:
              fetch-depth: 0
          name: Check out source-code repository

        - uses: oleksiyrudenko/gha-git-credentials@v1
          with:
               name: '{{ cookiecutter.github_username }}'
               email: '{{ cookiecutter.email }}'
               actor: '{% raw %}${{ github.actor }}{% endraw %}'
               token: '{% raw %}${{ secrets.QUBE_SYNC_TOKEN}}{% endraw %}'

        - name: Sync project
          run: qube sync . {% raw %}${{ secrets.QUBE_SYNC_TOKEN  }}{% endraw %} {% raw %}${{ github.repository_owner }}{% endraw %}
